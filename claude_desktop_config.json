{
  "_comment": "Add this server configuration to your Claude Desktop config file (%APPDATA%\\Claude\\claude_desktop_config.json)",
  "mcpServers": {
    "gym-engine": {
      "command": "cmd.exe",
      "args": [
        "/c",
        "pnpm",
        "--dir",
        "d:\\GYM",
        "run",
        "mcp"
      ],
      "env": {
        "DATABASE_URL": "file:gym.db",
        "NODE_ENV": "production"
      }
    }
  }
}
